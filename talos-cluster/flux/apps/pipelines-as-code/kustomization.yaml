apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
    - release.yaml
    - tailscale-ingress.yaml
    # - pipelines-as-code-secret.enc.yaml   # enable after GitHub App is created and secret encrypted (plan-0bn)
