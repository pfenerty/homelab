apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: tektonic-npm-pull-request
  namespace: tektonic-ci
spec:
  params:
    - name: project-name
      type: string
    - name: repo-full-name
      type: string
    - name: revision
      type: string
    - default: ./
      name: build-path
      type: string
    - name: url
      type: string
  tasks:
    - name: set-status-pending
      params:
        - name: repo-full-name
          value: $(params.repo-full-name)
        - name: revision
          value: $(params.revision)
      taskRef:
        kind: Task
        name: tektonic-set-status-pending
    - name: git-clone
      params:
        - name: url
          value: $(params.url)
        - name: revision
          value: $(params.revision)
      taskRef:
        kind: Task
        name: tektonic-git-clone
      workspaces:
        - name: workspace
          workspace: workspace
    - name: test-npm
      params:
        - name: build-path
          value: $(params.build-path)
        - name: repo-full-name
          value: $(params.repo-full-name)
        - name: revision
          value: $(params.revision)
      runAfter:
        - git-clone
        - set-status-pending
      taskRef:
        kind: Task
        name: tektonic-test-npm
      workspaces:
        - name: workspace
          workspace: workspace
    - name: build-npm
      params:
        - name: build-path
          value: $(params.build-path)
        - name: repo-full-name
          value: $(params.repo-full-name)
        - name: revision
          value: $(params.revision)
      runAfter:
        - test-npm
        - set-status-pending
      taskRef:
        kind: Task
        name: tektonic-build-npm
      workspaces:
        - name: workspace
          workspace: workspace
    - name: anchore-scan
      params:
        - name: repo-full-name
          value: $(params.repo-full-name)
        - name: revision
          value: $(params.revision)
      runAfter:
        - git-clone
        - set-status-pending
      taskRef:
        kind: Task
        name: tektonic-anchore-scan
      workspaces:
        - name: workspace
          workspace: workspace
  workspaces:
    - name: workspace
