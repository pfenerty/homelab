apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: trek
  namespace: flux-system
spec:
  interval: 30m
  chart:
    spec:
      chart: trek
      sourceRef:
        kind: HelmRepository
        name: trek
      version: 3.1.3
  values:
    ingress:
      enabled: true
      className: tailscale
      hosts:
        - host: trek
          paths:
            - /
      tls:
        - hosts:
            - trek
  targetNamespace: trek
